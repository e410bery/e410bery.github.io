# e410bery.github.io
# Ella Fortenbery's portfolio

This portfolio is built with React and Vite, then deployed as a static site to GitHub Pages.

## Local development

Install Node.js (the LTS version), then run:

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.
